// Backward-compatible alias. The old Notice of Consent has been folded into the
// combined Privacy & Terms document so any older import still opens the current notice.
export { default } from "./PrivacyTermsModal";
